# automatically generated by the FlatBuffers compiler, do not modify

# namespace: teleop

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class VRInput(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = VRInput()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsVRInput(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # VRInput
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # VRInput
    def Keypoints(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # VRInput
    def KeypointsAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float32Flags, o)
        return 0

    # VRInput
    def KeypointsLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # VRInput
    def KeypointsIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

    # VRInput
    def HandSide(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # VRInput
    def IsRelative(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # VRInput
    def Command(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # VRInput
    def Resolution(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

def VRInputStart(builder): builder.StartObject(5)
def Start(builder):
    return VRInputStart(builder)
def VRInputAddKeypoints(builder, keypoints): builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(keypoints), 0)
def AddKeypoints(builder, keypoints):
    return VRInputAddKeypoints(builder, keypoints)
def VRInputStartKeypointsVector(builder, numElems): return builder.StartVector(4, numElems, 4)
def StartKeypointsVector(builder, numElems):
    return VRInputStartKeypointsVector(builder, numElems)
def VRInputAddHandSide(builder, handSide): builder.PrependUint8Slot(1, handSide, 0)
def AddHandSide(builder, handSide):
    return VRInputAddHandSide(builder, handSide)
def VRInputAddIsRelative(builder, isRelative): builder.PrependUint8Slot(2, isRelative, 0)
def AddIsRelative(builder, isRelative):
    return VRInputAddIsRelative(builder, isRelative)
def VRInputAddCommand(builder, command): builder.PrependUint8Slot(3, command, 0)
def AddCommand(builder, command):
    return VRInputAddCommand(builder, command)
def VRInputAddResolution(builder, resolution): builder.PrependUint8Slot(4, resolution, 0)
def AddResolution(builder, resolution):
    return VRInputAddResolution(builder, resolution)
def VRInputEnd(builder): return builder.EndObject()
def End(builder):
    return VRInputEnd(builder)
try:
    from typing import List
except:
    pass

class VRInputT(object):

    # VRInputT
    def __init__(self):
        self.keypoints = None  # type: List[float]
        self.handSide = 0  # type: int
        self.isRelative = 0  # type: int
        self.command = 0  # type: int
        self.resolution = 0  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        vrinput = VRInput()
        vrinput.Init(buf, pos)
        return cls.InitFromObj(vrinput)

    @classmethod
    def InitFromObj(cls, vrinput):
        x = VRInputT()
        x._UnPack(vrinput)
        return x

    # VRInputT
    def _UnPack(self, vrinput):
        if vrinput is None:
            return
        if not vrinput.KeypointsIsNone():
            if np is None:
                self.keypoints = []
                for i in range(vrinput.KeypointsLength()):
                    self.keypoints.append(vrinput.Keypoints(i))
            else:
                self.keypoints = vrinput.KeypointsAsNumpy()
        self.handSide = vrinput.HandSide()
        self.isRelative = vrinput.IsRelative()
        self.command = vrinput.Command()
        self.resolution = vrinput.Resolution()

    # VRInputT
    def Pack(self, builder):
        if self.keypoints is not None:
            if np is not None and type(self.keypoints) is np.ndarray:
                keypoints = builder.CreateNumpyVector(self.keypoints)
            else:
                VRInputStartKeypointsVector(builder, len(self.keypoints))
                for i in reversed(range(len(self.keypoints))):
                    builder.PrependFloat32(self.keypoints[i])
                keypoints = builder.EndVector()
        VRInputStart(builder)
        if self.keypoints is not None:
            VRInputAddKeypoints(builder, keypoints)
        VRInputAddHandSide(builder, self.handSide)
        VRInputAddIsRelative(builder, self.isRelative)
        VRInputAddCommand(builder, self.command)
        VRInputAddResolution(builder, self.resolution)
        vrinput = VRInputEnd(builder)
        return vrinput
